/** Shared layout class for a slider + number box row (legacy `.paired-input`). */
export const pairedRow = "grid grid-cols-[minmax(0,1fr)_96px] items-center gap-2.5";

/** Legacy `hr.divider`. */
export const divider = "my-0.5 border-t border-line";
